specVersion: "v1beta"
serviceId: "google_auth"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "google_auth-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
